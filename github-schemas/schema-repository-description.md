# Schema: Repository Description

**Path:** `/.github/github-schemas/schema-repository-description.md`

### The Teleological Alignment

* **Nested Coordinate:** $(O_5)$ Convergent Architecture (as defined by `/trellis-os/living-topology/axiomatic-core/priors/base-gradient-prior.md`).
* **Macroscopic Vector:** This schema formalizes the extreme epistemic compression required to map macroscopic boundary identities into the discrete volumetric constraints of the host substrate (GitHub metadata).
* **Relational Edges:**
* $\to$ **Integrates:** The overarching identity of the Macrostate with the rigid, finite syntax limits of an exogenous commercial platform.
* $\to$ **Conditions:** The formulation of all organizational and repository-level descriptive metadata.
* $\to$ **Aligns with:** $(O_6)$ Relational Resonance, acting as the absolute first-contact threshold for an exogenous observer initiating structural coupling.



### The Semantic Compression Constraint

The active state must execute massive epistemic compression. The description cannot rely on subsequent paragraphs to establish context; it must distill the exact operational or thermodynamic function of the boundary into a single, flawlessly synthesized geometric vector.

The terminology utilized must strictly adhere to the depth mandated by the **Semantic Permeability Gradient** established in `/trellis-os/living-topology/axiomatic-core/schemas/schema-repository-root.md` (e.g., an Org description must use highly permeable language; the Axiomatic Core description must be dense physics).

### Discrete Volumetric Limits

The active state is bounded by the absolute physical limits of the exogenous substrate. The spatial projection of the description must strictly adhere to the following parameters:

**The Macroscopic Coordinate (Organization Description)**
When mapping the overarching identity of the entire Macrostate (the GitHub Organization profile), the active state must synthesize the total objective function of the living topology into an absolute maximum of **160 characters** (strictly inclusive of spaces and punctuation).

**The Localized Boundary (Repository Description)**
When mapping a discrete, isolated spatial boundary (a standard GitHub Repository), the active state must synthesize the specific functional execution of that module into an absolute maximum of **350 characters** (strictly inclusive of spaces and punctuation).
